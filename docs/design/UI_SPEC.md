# UI/UX 설계서 (UI_SPEC)

**작성자**: ui-designer
**날짜**: 2026-02-12
**버전**: 1.0

---

## 1. 디자인 원칙

1. **일관성**: 기존 시스템과 동일한 디자인 시스템 유지 (shadcn/ui, new-york 스타일)
2. **즉각성**: 사용자 액션에 즉각적인 피드백 제공 (toast, spinner)
3. **간결성**: 최소한의 클릭으로 목표 달성
4. **접근성**: 키보드 내비게이션, ARIA 라벨

---

## 2. 컬러 시스템

기존 시스템과 동일:
- Primary: `blue-600`
- Success: `green-600`
- Warning: `yellow-600`
- Danger: `red-600`
- Neutral: `gray-600`

---

## 3. 기능별 UI 설계

### 3.1 검색 및 필터링

#### 3.1.1 레이아웃

```
┌────────────────────────────────────────────────────────┐
│  📋 작업 목록                                           │
├────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────┐  ┌──────────┐│
│  │ 🔍 검색...           │  │  필터 ▼  │  │  초기화  ││
│  └──────────────────────┘  └──────────┘  └──────────┘│
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 업무 영역: [전체 ▼]  상태: [전체 ▼]  등급: [전체 ▼]│
│  │ 담당자: [전체 ▼]  기간: [YYYY-MM-DD ~ YYYY-MM-DD]│
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  ✅ 작업 1 (필터 매치)                            │
│  │  ✅ 작업 2 (필터 매치)                            │
│  └─────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

#### 3.1.2 컴포넌트 스펙

**SearchInput**:
- 컴포넌트: `Input` (shadcn/ui)
- 아이콘: `🔍` (왼쪽)
- Placeholder: "작업명이나 설명 검색..."
- debounce: 500ms
- 너비: `100%` (모바일), `400px` (데스크톱)

**FilterButton**:
- 컴포넌트: `Button` variant="outline"
- 아이콘: `Filter` (lucide-react)
- 클릭 시 필터 패널 토글 (collapsible)

**FilterPanel**:
- 컴포넌트: `Collapsible` (shadcn/ui)
- 배경: `bg-gray-50`
- 패딩: `p-4`
- 그리드: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`

**FilterSelect**:
- 컴포넌트: `Select` (shadcn/ui)
- 레이블: 위에 표시
- 기본값: "전체"

**DateRangePicker**:
- 컴포넌트: 2개의 `Input type="date"`
- 레이블: "시작일", "종료일"

**ResetButton**:
- 컴포넌트: `Button` variant="ghost"
- 텍스트: "초기화"
- 클릭 시 모든 필터 제거 + URL 쿼리 제거

#### 3.1.3 상태 표시

- **필터 활성화**: 버튼에 파란 점 표시
- **결과 수**: "총 45개 중 12개 표시"
- **빈 결과**: "검색 결과가 없습니다" 메시지

---

### 3.2 시간 추적 (타이머)

#### 3.2.1 레이아웃

**작업 카드 내 타이머**:

```
┌─────────────────────────────────────────────────┐
│  📋 API 개발                                     │
│  업무: 개발 | 마감: 2026-02-15 | A급              │
│                                                  │
│  ⏱️ 시간 추적                                    │
│  예상: 4시간 | 실제: 2시간 30분                  │
│                                                  │
│  ┌──────────────────────────────────────────┐  │
│  │  ▶️  02:30:15  ⏸️  ⏹️                     │
│  └──────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

#### 3.2.2 컴포넌트 스펙

**TimeTracker**:
- 위치: 작업 카드 하단
- 배경: `bg-blue-50` (타이머 실행 중), `bg-gray-50` (중지)
- 패딩: `p-3`
- 보더: `border-t` (구분선)

**타이머 디스플레이**:
- 폰트: `font-mono text-2xl`
- 형식: `HH:MM:SS`
- 색상: `text-blue-600` (실행 중), `text-gray-600` (중지)

**버튼**:
- **시작**: `▶️ 시작` (variant="default")
- **일시정지**: `⏸️` (variant="outline")
- **중지**: `⏹️ 중지` (variant="destructive", size="sm")

**상태 표시**:
- 실행 중: 애니메이션 펄스 효과 (`animate-pulse`)
- 일시정지: 노란색 테두리
- 중지: 회색

**시간 비교**:
- 표시: "예상: 4시간 | 실제: 2시간 30분"
- 초과 시: 빨간색 표시 "⚠️ 1시간 30분 초과"
- 아래 시: 초록색 표시 "✅ 1시간 30분 남음"

---

### 3.3 작업 템플릿

#### 3.3.1 템플릿 관리 페이지 (`/work/templates`)

```
┌────────────────────────────────────────────────────────┐
│  📑 작업 템플릿                                         │
│  자주 사용하는 작업 패턴을 저장하세요                   │
├────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐                              │
│  │ ➕ 새 템플릿 추가    │                              │
│  └──────────────────────┘                              │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  📋 API 개발                                     │  │
│  │  업무: 개발 | 예상: 4시간 | 우선순위: A          │  │
│  │  체크리스트: 3개                                 │  │
│  │  ┌─────────────┐  ┌─────────────┐              │  │
│  │  │ 작업 생성   │  │ 삭제        │              │  │
│  │  └─────────────┘  └─────────────┘              │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  🎨 디자인 작업                                  │  │
│  │  업무: 디자인 | 예상: 2시간 | 우선순위: B        │  │
│  │  체크리스트: 2개                                 │  │
│  │  ┌─────────────┐  ┌─────────────┐              │  │
│  │  │ 작업 생성   │  │ 삭제        │              │  │
│  │  └─────────────┘  └─────────────┘              │  │
│  └─────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

#### 3.3.2 템플릿 생성 폼

**Dialog 형태**:
- 컴포넌트: `Dialog` (shadcn/ui)
- 너비: `max-w-2xl`
- 트리거: "➕ 새 템플릿 추가" 버튼

**폼 필드**:
1. **템플릿명**: Input (필수)
2. **업무 영역**: Select (필수)
3. **예상 시간**: Input type="number" (시간 단위)
4. **우선순위**: Input type="number" (1-10)
5. **영향도**: Input type="number" (1-10)
6. **체크리스트**: Textarea (JSON 또는 줄바꿈 구분)
7. **설명**: Textarea (선택)

**버튼**:
- 저장: `Button` variant="default"
- 취소: `Button` variant="ghost"

#### 3.3.3 템플릿 카드

**레이아웃**:
- 배경: `bg-white`
- 보더: `border`
- 그림자: `shadow-sm`
- 패딩: `p-4`
- 호버: `hover:shadow-md` (transition)

**내용**:
- 아이콘: 업무 영역별 이모지
- 제목: `text-lg font-bold`
- 메타데이터: 작은 회색 텍스트
- 버튼: 하단에 배치

---

### 3.4 노트 및 첨부파일

#### 3.4.1 작업 상세 모달 확장

기존 작업 상세 모달에 "노트" 탭 추가:

```
┌────────────────────────────────────────────────────────┐
│  📋 API 개발                                            │
├────────────────────────────────────────────────────────┤
│  [상세] [노트] [히스토리]                               │
│                                                         │
│  📝 노트                                                │
│  ┌──────────────────────────────────────────────────┐ │
│  │ 마크다운 에디터                                   │ │
│  │                                                   │ │
│  │ - 요구사항 확인 완료                              │ │
│  │ - DB 스키마 설계 진행 중                          │ │
│  └──────────────────────────────────────────────────┘ │
│  ┌─────────┐                                          │
│  │ 저장    │                                          │
│  └─────────┘                                          │
│                                                         │
│  📎 첨부파일                                            │
│  ┌──────────────────────────────────────────────────┐ │
│  │  📄 요구사항.pdf (1.2 MB)  [다운로드] [삭제]      │ │
│  │  🖼️ 디자인.png (500 KB)   [다운로드] [삭제]      │ │
│  └──────────────────────────────────────────────────┘ │
│  ┌─────────────────────┐                              │
│  │ 📎 파일 선택         │                              │
│  └─────────────────────┘                              │
└────────────────────────────────────────────────────────┘
```

#### 3.4.2 노트 에디터

**컴포넌트**: `Textarea` (shadcn/ui)
- 높이: `min-h-[200px]`
- 폰트: `font-mono text-sm` (마크다운)
- Placeholder: "작업 노트를 입력하세요 (마크다운 지원)"
- 자동 저장: debounce 2초 후 저장 (draft 모드)
- 저장 버튼: 명시적 저장

**미리보기** (선택 사항):
- 탭: "편집" / "미리보기"
- 미리보기: ContentRenderer 사용 (기존 패턴)

#### 3.4.3 첨부파일 업로드

**파일 선택**:
- 컴포넌트: `Input type="file"` (숨김) + `Button` 트리거
- 레이블: "📎 파일 선택"
- 다중 선택: `multiple` 속성

**업로드 진행 표시**:
- 컴포넌트: `Progress` (shadcn/ui)
- 텍스트: "업로드 중... 45%"
- 완료: toast 알림

**첨부파일 목록**:
- 레이아웃: 리스트 형태
- 아이템:
  - 아이콘: 파일 타입별 (📄 PDF, 🖼️ 이미지, 📊 문서)
  - 파일명: 클릭 시 다운로드
  - 크기: 회색 작은 텍스트
  - 버튼: "다운로드", "삭제"

**빈 상태**:
- 텍스트: "첨부된 파일이 없습니다"
- 아이콘: 📎 (회색)

---

## 4. 반응형 디자인

### 4.1 브레이크포인트

- **모바일**: `< 768px`
  - 필터 패널: 1열 그리드
  - 타이머: 버튼 세로 배치
  - 템플릿 카드: 1열

- **태블릿**: `768px ~ 1024px`
  - 필터 패널: 2열 그리드
  - 타이머: 버튼 가로 배치
  - 템플릿 카드: 2열

- **데스크톱**: `> 1024px`
  - 필터 패널: 3열 그리드
  - 타이머: 버튼 가로 배치
  - 템플릿 카드: 3열

---

## 5. 인터랙션

### 5.1 애니메이션

- **타이머 실행**: 펄스 효과 (`animate-pulse`)
- **필터 패널**: 슬라이드 다운 (200ms)
- **토스트**: 페이드 인/아웃 (300ms)
- **카드 호버**: 그림자 증가 (150ms)

### 5.2 피드백

- **작업 생성**: toast "작업이 생성되었습니다"
- **타이머 시작**: toast "타이머 시작됨"
- **파일 업로드**: 진행바 + toast "파일 업로드 완료"
- **삭제 확인**: Dialog "정말 삭제하시겠습니까?"

---

## 6. 접근성 (a11y)

### 6.1 ARIA 라벨

- 검색 입력: `aria-label="작업 검색"`
- 타이머 버튼: `aria-label="타이머 시작"`
- 파일 업로드: `aria-label="파일 선택"`

### 6.2 키보드 내비게이션

- 필터: Tab으로 이동, Enter로 선택
- 타이머: Space로 시작/중지
- 템플릿: Enter로 작업 생성

### 6.3 색상 대비

- 모든 텍스트: WCAG AA 기준 (4.5:1 이상)
- 버튼: 명확한 포커스 링

---

## 7. 컴포넌트 리스트

### 7.1 신규 컴포넌트

1. `task-filter.tsx` (클라이언트)
2. `task-search.tsx` (클라이언트)
3. `time-tracker.tsx` (클라이언트)
4. `template-card.tsx`
5. `template-form.tsx` (클라이언트)
6. `task-note-editor.tsx` (클라이언트)
7. `task-attachment-upload.tsx` (클라이언트)
8. `task-attachment-list.tsx`

### 7.2 수정 컴포넌트

1. `task-card.tsx` — 타이머 추가, 노트/첨부 아이콘
2. `tasks/page.tsx` — 검색/필터 추가
3. `daily/page.tsx` — 검색/필터 추가

---

**승인 대기 중** ⏳
